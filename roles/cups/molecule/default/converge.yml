---
- name: Converge
  hosts: all
  gather_facts: true
  pre_tasks:
    - name: "Include mto79.system.cups"
      ansible.builtin.include_role:
        name: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | basename }}"

...
